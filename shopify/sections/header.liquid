<header class="bg-primary">
  <div class="container px-6 md:px-[45px] w-full h-[73px] flex items-center justify-between">
    <div class="flex items-center gap-[60px] h-full">
      <a id="logo" href="/">
        {% render 'logo-light.liquid' %}
      </a>

      {% render 'navbar' %}
      {% render 'navbar-mb' %}
    </div>

    <div class="flex items-center gap-4">
      <div class="items-center gap-2 hidden shipping-country">
        <span class="text-white">Ship to Country:</span>
        <div class="flex items-center gap-2 bg-white/30 text-white rounded-[8px] py-1 px-2">
          <img src="#" class="img-shipping-country w-[28px] h-[18px]">
          <span class="text-shipping-country"></span>
        </div>
      </div>
      <a href="/pages/settings" class="btn btn-icon-filled">
        {% render 'icon-setting.liquid' %}
      </a>
      {% render 'profile-dropdown' %}
      <button
        class="btn btn-icon-menu z-20 lg:hidden"
        data-collapse-toggle="navbar-hamburger"
        type="button"
      >
        {% render 'icon-menu' %}
      </button>
    </div>
  </div>
</header>
